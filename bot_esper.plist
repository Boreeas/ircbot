{
    host = irc.esper.net;
    port = 6667;
    nick = Feeder;
    user = Feeder;
    desc = "Public RSS Feed Bot";
    channels = ( #rss );

    access_owner = ( boreeas );

    access_admin = (  );

    access_mod = wug;

    plugins = (Core);
}